<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write your entry</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <h1>Journal Entries</h1>
    <ul>
        <% Entries.forEach( el => { %>
            <li class='entryItem' data-id='<%=el._id%>'>
                <span class='<%= el.read === true ? ' read ' : 'not '%>'><%= el.entry %></span>
                <span class='del'> Delete </span>
            </li>
            <% }) %>
    </ul>

    <h2>
        <%= user.userName %> has written
            <%= left %> journal entries.</h2>

    <form action="/Entries/createEntry" method='POST'>
        <input type="text" placeholder="Enter entry Item" name='entryItem'>
        <input type="submit">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>

</html>